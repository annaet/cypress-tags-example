{
  "name": "cypress-tags-example",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:smoke": "CYPRESS_TAGS=smoke cypress run",
    "cy:wip": "CYPRESS_TAGS=wip cypress run",
    "cy:not-wip": "CYPRESS_NOT_TAGS=wip cypress run",
    "cy:complex": "CYPRESS_TAGS=smoke,regression CYPRESS_NOT_TAGS=wip cypress run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^5.5.0"
  },
  "devDependencies": {
    "typescript": "^4.0.5"
  }
}
